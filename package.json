{
  "name": "wordsearch",
  "version": "1.0.0",
  "description": "A word search game built with Vite and deployed on Cloudflare Workers.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.42.1",
    "@cloudflare/workers-types": "^4.20260621.1",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "wrangler": "^4.103.0"
  }
}
